> **HUMAN REFERENCE ONLY** — This file preserves the full context, decisions, and intent
> behind this module. The Researcher agent reads this file to produce briefings.
> Other agents work from GitHub Issue tickets, not this file.

# <Module Name>

## Purpose

What this module does in one paragraph. Why it exists.

## Decisions Made During Grill

Document every significant decision and WHY it was made:

- **Decision:** [What was decided]
  **Why:** [The reasoning — constraints, tradeoffs, user preference]
  **Alternatives considered:** [What was rejected and why]

## Interface Contracts

### This Module Exposes

What other modules can call — function/endpoint signatures, return types:

- `functionName(params)` -> `returnType` — called by [ModuleX] when [trigger]

### This Module Consumes

What this module calls from other modules:

- `ModuleX.functionName(params)` — called when [trigger]. If unavailable: [fallback]

## Features

Detailed feature specs from the Module Grill.

## User Stories

- As [who], I want [what], so that [why].

## Workflows

### Workflow 1: <Name>
1. Step
2. Step
3. Step

## Business Rules

Constraints that affect implementation:

- [Rule]: [Context]

## Edge Cases

Things that came up during the grill that aren't obvious from the tickets:

- [Edge case]: [How it should be handled and why]

## Data Model

Tables, fields, relationships, indexes owned by this module.
